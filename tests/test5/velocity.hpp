#pragma once
#include <iostream>
#include "length.hpp"

// double velocity(int *length, int count, int timer)
double velocity(int timer)
{
    // double vel = 0.0;
    // int summ = 0;
    // int * length = getLength(count);
    // int length = getLength();
    // for(int i = 0; i < count; i++)
    // {
        // summ += *(length + i);
        // std::cout << "summ = " << summ << std::endl;
    // }
    // vel = ;

    return getLength() / timer;
}